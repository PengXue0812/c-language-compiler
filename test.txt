//#include<stdio.h>


int main() {
    int a = 50;
    int sum = -1;
    // outer loop
    for (int i = 0; i < a * 2; i=i+1) {
        sum = sum + i;
    }

    // print the value of `sum`
    print(sum);

    return 0;
}